# python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
*.egg-info/

# env files
.env
.env.local
.env.*.local

# OS
.DS_Store
Thumbs.db

# editor
.vscode/
.idea/

# 개입 원장. 실행마다 쌓이는 런타임 상태라 커밋하지 않는다.
# 실제 규모에서는 파일이 아니라 공유 저장소에 둔다 — 여러 프로세스가 같이 봐야
# "한 단위 한 개입" 이 성립한다.
*.db

# 임베딩 캐시. 실행하면 다시 만들어지는 산출물이라 저장소에 둘 것이 아니다.
.embed_cache/
