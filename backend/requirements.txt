# ── OpenAPI 산출물에 영향을 주는 것은 **정확히** 고정한다 ────────────────
# openapi.json 을 커밋해 두고 CI 가 드리프트를 막는 구조인데, 생성기 버전이 안
# 묶여 있으면 그 검사가 성립하지 않는다. 같은 코드라도 FastAPI·Pydantic 버전이
# 다르면 스키마 모양이 달라진다(HTTPValidationError 유무, description 필드 등).
# 실제로 CI 는 이것 때문에 처음부터 계속 빨간불이었다 — 코드가 아니라 환경이
# 달라서 생긴 차이였다.
fastapi==0.115.6
starlette==0.41.3
uvicorn>=0.34
pydantic==2.13.3
langgraph>=0.3
# 개입 원장. **한 단위 한 개입**을 애플리케이션이 아니라 DB 가 보장해야 해서
# (부분 유니크 인덱스) 필요하다 — 두 프로세스가 경합하면 애플리케이션 검사는 진다.
sqlalchemy==2.0.36
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27          # TestClient

# 정책 문서 검색 코어 — RAG-Marketing 저장소에서 재사용한다.
# 두 시스템이 같은 검색 구현을 공유하며, 중복 구현하지 않는다.
marketplace-retrieval @ git+https://github.com/MoonSuhyeon/RAG-Marketing.git
